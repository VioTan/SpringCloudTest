eureka:
  client:
    serviceUrl:
      defaultZont: http://localhost:8761/eureka/
server:
  port: 8902
spring:
  application:
    name: service-feign
## Feign应用中使用断路器，必须添加，因为默认是禁止的
feign:
  hystrix:
    enabled: true
